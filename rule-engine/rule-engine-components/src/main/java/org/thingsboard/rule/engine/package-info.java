/**
 * Built-in rule chain nodes (filter, transform, action, flow, external integrations).
 * Each {@code Tb*Node} class is annotated with {@link org.thingsboard.rule.engine.api.RuleNode}.
 */
package org.thingsboard.rule.engine;
