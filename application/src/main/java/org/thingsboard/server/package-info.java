/**
 * ThingsBoard core server application ({@link org.thingsboard.server.ThingsboardServerApplication}).
 *
 * <p>Subpackages:
 * <ul>
 *   <li>{@code controller} — HTTP REST API</li>
 *   <li>{@code service} — business logic, security, queues, WebSockets</li>
 *   <li>{@code actors} — actor-system processing for devices, rule chains, calculated fields</li>
 *   <li>{@code config} — Spring configuration</li>
 *   <li>{@code install} — database install/upgrade orchestration</li>
 * </ul>
 *
 * <p>Module documentation: {@code application/README.md} in the repository.
 */
package org.thingsboard.server;
